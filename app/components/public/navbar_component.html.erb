<nav class="bg-white fixed w-full z-20 top-0 inset-s-0 border-b border-gray-200">
  <div class="max-w-7xl flex flex-wrap items-center justify-between mx-auto p-4">
    <%= link_to helpers.root_path, class: "flex items-center space-x-3 rtl:space-x-reverse" do %>
      <%= image_tag "logo/color.png", class: "w-12" %>
      <span class="self-center text-2xl font-semibold whitespace-nowrap text-gray-800">AlgoByte</span>
    <% end %>
    <div class="flex md:order-2 space-x-3 md:space-x-0 rtl:space-x-reverse">
      <%= link_to "Start a Project", "#", class: "hidden md:inline-block bg-white text-gray-800 border border-gray-800 px-6 py-3 rounded transition duration-300 hover:-rotate-5 hover:shadow-lg hover:shadow-orange-600" %>
      <button data-collapse-toggle="navbar-cta" type="button"
              class="inline-flex items-center p-2 w-9 h-9 justify-center text-sm text-gray-600 rounded-lg md:hidden hover:bg-gray-100 focus:outline-none focus:ring-2 focus:ring-gray-200"
              aria-controls="navbar-cta" aria-expanded="false">
        <span class="sr-only">Open main menu</span>
        <svg class="w-5 h-5" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 17 14">
          <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M1 1h15M1 7h15M1 13h15"/>
        </svg>
      </button>
    </div>
    <div class="items-center justify-between hidden w-full md:flex md:w-auto md:order-1" id="navbar-cta">
      <ul class="font-medium flex flex-col mt-4 md:flex-row md:space-x-8 rtl:space-x-reverse md:mt-0 md:border-0 md:bg-white">
        <% links.each do |link| %>
          <li class="flex flex-col gap-4">
            <%= link_to link.label, link.path,
                class: "block py-2 px-3 text-gray-700 rounded hover:bg-gray-100 md:hover:bg-transparent md:hover:text-orange-600 md:p-0 transition-all duration-300" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
